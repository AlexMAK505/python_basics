# Входное шестизначное число
number = 123321  # можно заменить на любое другое шестизначное число

# Преобразуем число в строку, чтобы получить отдельные цифры
s = str(number)

# Проверим, что число действительно шестизначное
if len(s) != 6:
    print("Номер билета должен быть шестизначным!")
else:
    # Берём первые три и последние три цифры
    first_half = s[:3]
    second_half = s[3:]

    # Считаем сумму цифр в каждой половине
    sum_first = sum(int(digit) for digit in first_half)
    sum_second = sum(int(digit) for digit in second_half)

    # Сравниваем суммы
    if sum_first == sum_second:
        print("Счастливый билет")
    else:
        print("Несчастливый билет")
