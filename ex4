# Входные данные (можно менять для тестирования)
width = 15
length = 55
height = 15

# Преобразуем в список для удобства
dimensions = [width, length, height]

# Проверяем правила в правильном порядке (важен приоритет!)

# 1. Если хотя бы одно измерение > 200 см → "Упаковка для лыж"
if any(d > 200 for d in dimensions):
    print("Упаковка для лыж")

# 2. Если все измерения <= 15 см → "Коробка №1"
elif all(d <= 15 for d in dimensions):
    print("Коробка №1")

# 3. Если хотя бы одно измерение > 15 и < 50 → "Коробка №2"
# Но при этом ни одно измерение не больше 200 (иначе сработало бы правило 1),
# и не все <= 15 (иначе сработало бы правило 2)
elif any(15 < d < 50 for d in dimensions):
    print("Коробка №2")

# 4. Во всех остальных случаях → "Коробка №3"
else:
    print("Коробка №3")
